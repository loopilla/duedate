import { DueDateApplication } from '../src/dueDate';
import { expect } from 'chai';
import * as mocha from 'mocha';

describe('App test', () => {
    // let app: DueDateApplication;
    // const goodParams = ['', '', '2020-02-10T13:25', '23'];

    // beforeEach(() => {
    //     app = new DueDateApplication(goodParams);
    // });

    // it('app can be initiated', () => {
    //     expect(!!new DueDateApplication(['2020-02-10T13:25', '23'])).to.be.true;
    // })
});
